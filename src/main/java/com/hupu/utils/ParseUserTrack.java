package com.hupu.utils;

import org.apache.flink.api.common.functions.MapFunction;

public class ParseUserTrack implements MapFunction {

    @Override
    public Object map(Object o) throws Exception {
        return null;
    }
}
